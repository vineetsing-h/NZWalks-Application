{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // We create ConnectionString to the database in appsettings.json file
  // So let's create new object called connectionstrings
  "ConnectionStrings": {
    // Inside this object we will have key value pair
    "NZWalksConnectionString": "Server=VINEET\\MSSQLSERVER01;Database=NZWalksDb;Trusted_Connection=True;TrustServerCertificate=True"
  }
}
